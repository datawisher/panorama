server:
  port: 8810
  servlet:
    context-path: /api

spring:
  redis:
    database: 0
    host: localhost
    port: 6379
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
        min-idle: 0
msg:
  enabled: true
  url: 127.0.0.1
  access-key-id: 10001
  access-key-secret: xidd-dics-ss992dx
